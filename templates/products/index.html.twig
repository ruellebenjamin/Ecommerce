{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <div class="flex flex-wrap gap-20">
        <div class="w-1/3">
            <img class="w-full" src="/images/products/{{product.imageName}}" alt="image">
        </div>
        <div >
            
            <p class="font-bold text-4xl">{{product.name}}</p>
            <p class="text-2xl">{{product.description}}</p>
            <p class="text-2xl">{{product.price}} €</p>
            <a href="">Ajouter au panier</a>
        </div>
    </div>
{% endblock %}
